default:
  paths:
    features: features
    bootstrap: %behat.paths.features%/contexts
  context:
    class: Silo\Context\FeatureContext
    parameters:
      debugDoctrine: false
      coverage: false

coverage:
  paths:
    features: features
    bootstrap: %behat.paths.features%/contexts
  context:
    class: Silo\Context\FeatureContext
    parameters:
      debugDoctrine: false
      coverage: true